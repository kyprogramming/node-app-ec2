    pipeline {
    agent any
    stages {
       
        stage('Build') {
            steps {
                // Install dependencies and build Node.js application
                sh 'npm install'
            }
        }
    }
}
